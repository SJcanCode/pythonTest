name: build
on:
  push:
  pull_request:
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Install snapd 
        run:  sudo -S apt install snapd -y
      - name: Uninstall snapd 
        run:  sudo -S apt autoremove --purge snapd -y
